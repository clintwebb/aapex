#------------------------------------------
# This config is managed by Ansible.
#------------------------------------------
http_port 3128
acl QUERY urlpath_regex cgi-bin \?
cache deny QUERY
refresh_pattern ^ftp:           1440    20%     10080
refresh_pattern ^gopher:        1440    0%      1440
refresh_pattern .               0       20%     4320
acl manager proto cache_object
acl SSL_ports port 443 8088     # https
acl Safe_ports port 80          # http
acl Safe_ports port 21          # ftp
acl Safe_ports port 443         # https
acl Safe_ports port 8088        # SAM Subscription manager
acl ftp proto FTP               # FTP protocol
acl CONNECT method CONNECT
http_access allow manager localhost
http_access deny manager
http_access allow ftp
http_access deny !Safe_ports
http_access deny CONNECT !SSL_ports
http_access allow localhost

# Logging with XFF or normal source, excluding F5 self IPs
acl f5_selfIPs_test     src  172.26.15.2-172.26.15.6
acl has-xff req_header X-Forwarded-For ^(([0-9]+\.[0-9]+\.[0-9]+\.[0-9]+)|(\[([0-9a-f]+)?:([0-9a-f:]+)?:([0-9a-f]+|0-9\.]+)?\]))
logformat squid      %ts.%03tu %6tr %>a %Ss/%03>Hs %<st %rm %ru %[un %Sh/%<a %mt
logformat squid-xff  %ts.%03tu %6tr %{X-Forwarded-For}>h %Ss/%03>Hs %<st %rm %ru %[un %Sh/%<a %mt
access_log /var/log/squid/access.log squid-xff has-xff
access_log /var/log/squid/access.log squid !has-xff !f5_selfIPs_test

# Exceptions include URLS that would normally match good_dstdom URLS
acl exceptions  dstdom_regex     ^mail\.google\.com$ ^drive\.google\.com$

# URL used for loadbalancer health checks
acl f5_monitor_url  dstdom_regex     ^www\.google\.com$

# URL Whitelist
acl good_dstdom      dstdom_regex  "/etc/squid/dstdom_regex"
acl brokerdb_dstdom  dstdom_regex  "/etc/squid/dstdom_brokerdb_regex"
acl epelrepos urlpath_regex epel/7/x86_64

# True source IP whitelisting, derived from F5 injected XFF Header
acl coretx_app_test req_header X-Forwarded-For ^(172\.26\.28\.[0-9]+)
acl coretx_web_test req_header X-Forwarded-For ^(172\.26\.29\.[0-9]+)
acl coretx_db_test  req_header X-Forwarded-For ^(172\.26\.30\.[0-9]+)
acl shyapps_test    req_header X-Forwarded-For ^(172\.26\.15\.[0-9]+)
acl sysmgt          req_header X-Forwarded-For ^(172\.24\.251\.[0-9]+)
acl brokerdb        req_header X-Forwarded-For ^(172\.26\.15\.71)

# Rule enforcement
http_access deny exceptions
http_access allow coretx_app_test   good_dstdom
http_access allow coretx_web_test   good_dstdom
http_access allow coretx_db_test    good_dstdom
http_access allow shyapps_test      good_dstdom
http_access allow sysmgt
http_access allow shyapps_test      epelrepos
http_access allow f5_selfIPs_test   f5_monitor_url
http_access deny                    good_dstdom
http_access deny all
http_reply_access allow all
icp_access allow all
coredump_dir /var/spool/squid

# Setting preference for IPv4 DNS as we dont have IPv6 networks and may cause resolution issues.
dns_v4_first on
